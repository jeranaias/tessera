# Built binaries — anchored to repo root so they never match cmd/<name>/ SOURCE dirs
/tessera
/tessera.exe
*.exe

# Signing keys — NEVER commit private keys
*.key
*.key.pub
*-ed25519.key
*-ed25519.key.pub

# Generated receipts / chains
receipt.json
*.receipt.json
*.chain

# Go
/vendor/
*.test
*.out

# Python
__pycache__/
*.pyc
